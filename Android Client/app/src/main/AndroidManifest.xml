<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mapema.shuttercontroller">

    <uses-permission android:name="android.permission.INTERNET" >
    </uses-permission>

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" >
    </uses-permission>

    <application
        android:allowBackup="false"
        android:icon="@mipmap/shutter_72x72_rounded"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/shutter_72x72_rounded"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name="com.mapema.shuttercontroller.AppContext">
        <activity android:name="com.mapema.shuttercontroller.activities.MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>